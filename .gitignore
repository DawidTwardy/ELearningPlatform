# -----------------------------------------------------------------------------
# ZALEŻNOŚCI I PLIKI TYMCZASOWE (Frontend)
# -----------------------------------------------------------------------------

# Node modules (zarówno w głównym jak i w podkatalogach)
node_modules/
/Frontend/node_modules

# Logi błędów NPM/Yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp
.pnp.js

# Build produkcji Frontend
/build
/dist
/out
/Frontend/build
/Frontend/dist
/Frontend/out
.next/
coverage/

# -----------------------------------------------------------------------------
# .NET / ASP.NET CORE (Backend)
# -----------------------------------------------------------------------------

# Wyniki kompilacji (niezależnie od poziomu zagnieżdżenia)
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
x64/
x86/

# Pliki tymczasowe i cache
[Tt]emp/
*.suo
*.user
*.userosscamp
*.sln.docstates

# Pakiety NuGet (jeśli nie używasz Package Reference)
packages/
*.nupkg
project.lock.json
project.assets.json

# Pliki generowane przez Entity Framework / Narzędzia
# UWAGA: Folder 'Migrations' powinien być w Gicie, więc go NIE ignorujemy.

# -----------------------------------------------------------------------------
# UPLOADS / PLIKI UŻYTKOWNIKA (To co dodaliśmy)
# -----------------------------------------------------------------------------
# Ignorujemy zawartość folderu uploads, ale chcemy sam folder (jeśli dodasz .gitkeep)
**/wwwroot/uploads/*
!**/wwwroot/uploads/.gitkeep

# -----------------------------------------------------------------------------
# IDE i SYSTEM
# -----------------------------------------------------------------------------
.idea/
.vscode/
.vs/
*.swp
Thumbs.db
.DS_Store

# -----------------------------------------------------------------------------
# BEZPIECZEŃSTWO (Zmienne środowiskowe)
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local

# Opcjonalnie: ignoruj lokalne ustawienia developerskie .NET (jeśli masz tam hasła)
appsettings.Development.json